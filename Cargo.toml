[package]
name = "shf"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7.7", features = ["ws"] }

tokio = { version = "1.41.1", features = [
    "rt-multi-thread",
    "macros",
    "signal",
    "fs",
] }
tower-http = { version = "0.6.2", features = ["fs"] }
